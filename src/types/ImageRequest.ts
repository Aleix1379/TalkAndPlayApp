export interface ImageRequest {
    base64: string
    name: string
}

export interface ImageResponse {
    id: number
    name: string
}

export interface Avatar {
    id: number
    name: string
    // user: User
}
